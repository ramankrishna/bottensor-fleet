"""Bundled example graphs.

These mirror the top-level ``examples/`` directory and are shipped inside the
wheel so users can list and extract them with ``fleet examples``. The top-level
copies remain the canonical source for hand-editing; this package is consumed
via :mod:`importlib.resources` at runtime.
"""
